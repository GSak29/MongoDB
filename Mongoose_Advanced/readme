1.npm init -y

2.npm i nodemon --save-dev

3.next got to "package.json"
        convert :
                "scripts": {
                    "test": "echo \"Error: no test specified\" && exit 1",
                },
            
        to: 
                "scripts": {
                    "test": "echo \"Error: no test specified\" && exit 1",
                    "dev": "nodemon script.js"
                },